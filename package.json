{
  "name": "ai-resume",
  "version": "1.0.0",
  "private": true,
  "description": "Eric Backman's AI-native resume: an MCP server at ai.ericbackman.com that AI assistants can query",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "npm run types && tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
